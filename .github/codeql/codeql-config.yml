# https://github.com/github/codeql-action
# https://github.com/github/codeql-go for experimental queries

name: CodeQL Configuration

# limit custom queries and packs to specified paths
# paths:
  # - 'src'

# disable-default-queries: true

queries:
  # will fail if the folder does not exists or is empty
  # - name: Custom CodeQL Queries
    # uses: './.github/codeql/queries/'
  # - uses: security-extended
  # security-and-quality inherits security-extended
  - uses: security-and-quality
  # - name: Experimental CodeQL Queries
    # uses: './.github/codeql/experimental.qls'
